services:
  nginx:
    ports:
      - "8080:80"
      - "80:80"
      - "443:443"
    volumes:
      - /home/nginx/certs:/etc/nginx/certs:ro
    networks:
      - web

networks:
  web:
    external: true
    name: web
