location = /todo {
    return 308 /todo/;
}

location /todo/ {
    root /usr/share/nginx/html;
    try_files /todo-app.html =404;
    default_type text/html;
    add_header Cache-Control "no-cache" always;
}
