[project]
name = "paiguangguang"
version = "0.1.0"
description = "Personal portfolio frontend with a minimal health-check service"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115,<0.117",
    "uvicorn[standard]>=0.30,<0.32",
]

[dependency-groups]
dev = ["pytest>=8.2,<9", "httpx>=0.27,<0.29"]
